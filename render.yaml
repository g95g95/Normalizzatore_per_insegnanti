services:
  - type: static
    name: grade-alchemy
    staticPublishPath: client/dist
    buildCommand: npm install && npm run build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
